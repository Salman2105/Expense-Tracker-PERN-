Fix my Prisma production seeding deployment issue.

My stack is Node.js + Express + TypeScript/JavaScript + Prisma + PostgreSQL, and my production database is Neon. I deploy the backend on Render.

The Render logs show:

Error: Command failed with ENOENT: tsx prisma/seed.ts
spawn tsx ENOENT

The current production command is:

npx prisma generate && npx prisma migrate deploy && npx prisma db seed && node server.js

Prisma successfully loads the schema, but `prisma db seed` fails because Render cannot find the `tsx` executable.

Please inspect my existing project before making changes, especially:
- package.json
- prisma.config.ts
- prisma/schema.prisma
- prisma/seed.ts
- tsconfig.json
- Render build/start configuration if available

Implement the cleanest production-ready fix.

Requirements:

1. Determine why `tsx` is unavailable during the Render deployment.
2. If `tsx` is required by the existing `prisma/seed.ts`, make sure `tsx` is correctly available in the production environment.
3. Prefer adding `tsx` to the appropriate `dependencies`/`devDependencies` based on how Render installs packages. Do not blindly change dependency configuration.
4. Alternatively, if the project architecture allows it, convert the seed to a production-compatible JavaScript seed without introducing unnecessary dependencies. Choose the cleaner solution based on the existing project.
5. Keep Prisma's existing seed configuration compatible with the Prisma version already installed.
6. Make the seed idempotent. Running `npx prisma db seed` multiple times must NOT create duplicate global categories.
7. Use Prisma `upsert` or another appropriate approach based on the existing Category schema and unique constraints.
8. Do not delete, reset, or overwrite existing user-created/custom categories.
9. Do not use `prisma migrate reset` or any destructive database command.
10. Keep the existing Neon production DATABASE_URL supplied through Render environment variables. Never hardcode credentials.
11. Keep this production command working:

npx prisma generate && npx prisma migrate deploy && npx prisma db seed && node server.js

12. Make sure `node server.js` still starts the Express server normally after successful seeding.
13. Do not modify unrelated application functionality.

After making the changes, verify:
- Prisma generate works
- Prisma migrate deploy works
- Prisma db seed works
- The seed can run multiple times without duplicates
- The application starts successfully

Finally, tell me exactly:
- Which files you changed
- What you changed
- Whether `tsx` was added/moved
- What Render Build Command should be
- What Render Start Command should be
- Any environment variables required
- The exact command I should run locally to test the seed before redeploying

IMPORTANT:
Do not use a local database for production seeding. The production seed must run against the Neon database through Render's DATABASE_URL.