Fix the mobile responsiveness issue with all Category and Type dropdown/select fields in the application.

I have a mobile UI issue where opening a native <select> dropdown causes the options panel to overflow outside the form/modal and extend beyond the visible screen, as shown in the attached screenshot.

This issue currently occurs in these 4 places:

1. Transaction Creation Form
   - Category field
   - Type field

2. Category Creation Form
   - Category field
   - Type field

GOAL:
Make all four dropdowns fully mobile-friendly while preserving the existing desktop UI, colors, typography, spacing, animations, and overall design.

IMPORTANT:
Do not implement a one-off fix for only one component. Find the shared/reusable select/dropdown component or common styling used by these fields and fix the underlying implementation so the behavior is consistent everywhere.

Requirements:

1. MOBILE RESPONSIVENESS
   - On small screens, every select/dropdown must remain within the available viewport width.
   - The select element itself must never overflow its parent container.
   - Prevent horizontal page scrolling caused by these fields.
   - Use:
     - width: 100%
     - max-width: 100%
     - min-width: 0
     - box-sizing: border-box
   where appropriate.
   - Make sure the parent form/modal/container also uses responsive width constraints.

2. DROPDOWN OPTIONS
   - Long category names such as "Uncategorized", "Entertainment", etc. must not break the mobile layout.
   - Ensure option text does not force the select/container to become wider than the screen.
   - If native <select> behavior cannot reliably control the opened options popup on mobile browsers, do NOT fight the browser's native popup with excessive CSS.
   - In that case, implement a lightweight accessible custom dropdown/select component specifically for mobile/responsive behavior while keeping the normal/native-looking experience on desktop.
   - The custom dropdown must support keyboard navigation, focus states, Escape to close, Enter/Space selection, and proper ARIA attributes.

3. MODAL / FORM CONTAINMENT
   - Check the Transaction Creation modal/form and Category Creation modal/form.
   - Ensure dropdowns cannot visually force the modal wider than the viewport.
   - Add appropriate overflow handling where necessary.
   - Do not simply use overflow:hidden as the only fix if it causes dropdown options to become inaccessible.

4. CATEGORY FIELD
   - Preserve all existing category options and functionality.
   - Global categories and user-created categories must continue working.
   - Do not change API contracts, backend logic, database models, or category values.

5. TYPE FIELD
   - Preserve the existing transaction/category type values and behavior.
   - Do not change enum values or backend payloads.

6. DESKTOP
   - Do NOT unnecessarily redesign the desktop version.
   - Existing desktop appearance should remain visually unchanged.
   - The fix should primarily improve tablet/mobile responsiveness.

7. RESPONSIVE BREAKPOINTS
   Test at least:
   - 320px width
   - 360px width
   - 375px width
   - 390px width
   - 414px width
   - 768px width
   - normal desktop width

8. CHECK FOR DUPLICATE IMPLEMENTATIONS
   Search the entire frontend for:
   - <select
   - category selects
   - type selects
   - reusable Select components
   - Transaction creation form
   - Category creation form

   Identify whether these four fields share a component or styling.
   If there is a shared component, fix it there instead of duplicating CSS.

9. VISUAL REQUIREMENTS
   Keep the existing design system:
   - Existing primary color
   - Existing CSS variables
   - Existing Tailwind/CSS architecture
   - Existing border radius
   - Existing typography
   - Existing focus/hover states
   - Existing modal design
   - Existing animations

10. ACCESSIBILITY
   - Preserve proper labels for every select.
   - Ensure keyboard accessibility.
   - Ensure visible focus states.
   - Do not remove native accessibility behavior unless replacing it with an equivalent accessible implementation.
   - Ensure touch targets are comfortable on mobile.

11. VALIDATION
   After implementation:
   - Run TypeScript/type checking.
   - Run frontend lint/tests if available.
   - Run production build.
   - Verify all four dropdowns.
   - Verify category selection still sends the correct category ID/value.
   - Verify type selection still sends the correct enum/value.
   - Verify create/edit flows still work.
   - Verify there is no horizontal overflow on mobile.

IMPORTANT:
Do not modify backend APIs, database schema, authentication, category logic, or transaction logic unless absolutely required. This is a frontend responsive UI/UX fix.

Before making changes, inspect the existing implementation and determine the shared/root cause. Then implement the cleanest reusable solution rather than adding separate hacks to each of the four fields.

At the end, provide:
1. Root cause
2. Files/components changed
3. What responsive solution was implemented
4. Validation/tests performed
5. Confirmation that all 4 dropdowns were fixed