I have a Node.js + Express + Prisma + PostgreSQL backend, and my production database is hosted on Neon. I already have a Prisma seed script that creates my global/default categories.

The issue is that I ran `npx prisma db seed` locally, so the categories exist in my local/development database, but they are missing from the production Neon database after deployment.

Please inspect my existing project and implement a production-ready solution for Prisma seeding.

Requirements:

1. Inspect my existing:

   * `prisma/schema.prisma`
   * Prisma seed file (`prisma/seed.js`, `prisma/seed.ts`, or whatever is configured)
   * `package.json`
   * `.env` / environment configuration
   * Prisma configuration
   * deployment configuration

2. DO NOT hardcode or expose any production Neon credentials.

3. Ensure the existing seed script correctly creates all global categories in the production database.

4. Make the seed script IDEMPOTENT:

   * Running the seed multiple times must not create duplicate global categories.
   * Use `upsert` or another appropriate Prisma-safe approach based on the existing schema and unique constraints.
   * Do not modify or delete existing user-created/custom categories.
   * Only seed the intended global/default categories.

5. Ensure Prisma migrations are production-safe:

   * Use `npx prisma migrate deploy` for production migrations.
   * Do not use `prisma migrate dev` in production.

6. Configure the deployment process so the production Neon database can be initialized correctly.

   * If appropriate for this project's architecture, update the Render/deployment build/start configuration to run:
     `npx prisma generate`
     `npx prisma migrate deploy`
     `npx prisma db seed`
   * Do not blindly run destructive commands such as `prisma migrate reset`.
   * Do not seed the database on every deployment if that could be unsafe; only do so if the seed is fully idempotent.

7. Make sure Prisma's seed command is correctly configured in `package.json` / Prisma configuration according to the Prisma version already used by this project.

8. Verify that the Category schema and seed logic correctly distinguish:

   * Global categories
   * User-specific/custom categories
   * The default `Uncategorized` category, if it already exists in the project

9. Do not change unrelated backend functionality or database models.

10. After implementation, provide:

* Exact files changed
* Exact commands I need to run
* Exact Render deployment/build configuration changes, if required
* How I can verify the categories exist in my Neon production database
* Any environment variables that must be configured in Render

IMPORTANT:
The production `DATABASE_URL` must come from the Render environment variables and point to my production Neon database. Never replace it with a local database URL and never commit credentials to Git.

Before making changes, inspect the existing project structure and preserve the current architecture and conventions. Do not create a new seeding system if an existing Prisma seed system can be safely fixed.
