You are working on my existing Expense Tracker backend.

My deployment target is **Render Free Web Service**, with:

* PostgreSQL hosted on Neon
* Prisma ORM
* Cloudinary for profile-image storage
* GitHub as the source repository

I have already:

1. Pushed the project to GitHub.
2. Created and configured my Neon PostgreSQL project.
3. Integrated Cloudinary.
4. Neon has populated `.env.local` with `DATABASE_URL`, `DATABASE_URL_UNPOOLED`, and `NEON_BRANCH`.

Before I deploy the backend to Render, perform a **production deployment readiness audit and preparation**.

### IMPORTANT SAFETY RULES

* Do NOT expose, print, commit, or hardcode any secret values.
* Do NOT modify or delete my production database.
* Do NOT run destructive database commands.
* Do NOT run `prisma migrate reset`.
* Do NOT use `prisma db push` for production.
* Do NOT commit `.env`, `.env.local`, or any file containing secrets.
* Do NOT replace existing working functionality unnecessarily.
* Preserve the current API contracts and frontend compatibility.
* Make changes only when necessary for production deployment.

### 1. Environment configuration

Inspect the backend and identify every environment variable actually required by the application.

Verify that secrets are loaded through `process.env`.

Make sure production configuration supports:

* `NODE_ENV`
* `DATABASE_URL`
* `JWT_SECRET`
* Cloudinary credentials
* frontend/client URL used by CORS
* any other environment variables already required by the application

Do NOT invent unnecessary variables.

Create or update an appropriate `.env.example` containing variable names only, for example:

DATABASE_URL=
JWT_SECRET=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLIENT_URL=
NODE_ENV=production

Never put real credentials in `.env.example`.

### 2. Git safety

Inspect `.gitignore`.

Make sure it excludes:

.env
.env.*
!.env.example

Also ensure generated/local files such as:

* node_modules
* Prisma local/generated artifacts where appropriate
* logs
* local environment files

are not accidentally committed.

If any secret-containing file is currently tracked by Git, STOP and report it instead of exposing or rewriting the secret.

### 3. Prisma production readiness

Inspect the Prisma configuration and migrations.

Verify that:

* PostgreSQL is correctly configured.
* Existing migrations are present.
* Prisma Client generation works.
* The project can use Neon in production.
* No destructive migration command is required.

The production migration command must be:

npx prisma migrate deploy

Do NOT use:

npx prisma migrate dev

Do NOT use:

npx prisma db push

Do NOT reset the database.

Verify the appropriate build/deployment commands for Render.

### 4. package.json

Inspect the backend `package.json`.

Ensure the backend has a valid production start command.

Prefer:

npm start

with the existing production server entry point.

Do not change the application entry point unless necessary.

Verify that all runtime dependencies are in `dependencies`, not incorrectly placed only in `devDependencies`.

### 5. Render compatibility

Verify that the Express server listens on Render's assigned port.

The application should use:

process.env.PORT

with a safe local fallback such as:

3000

Do not hardcode a production-only port.

Also verify the server binds correctly for cloud deployment.

### 6. Health endpoint

Check whether a simple health endpoint already exists.

If it does not exist, add:

GET /health

It should return a lightweight response such as:

{
"success": true,
"message": "API is healthy"
}

It must not expose:

* database credentials
* JWT secrets
* Cloudinary secrets
* internal filesystem information
* sensitive user information

### 7. CORS

Inspect the existing CORS implementation.

Do not use:

origin: "*"

because this application uses authentication.

Prepare CORS so the production frontend URL can be supplied through an environment variable such as:

CLIENT_URL

It should still be possible to run the backend locally during development.

Do not hardcode a future Render/Vercel frontend URL because it does not exist yet.

### 8. Authentication/security

Review the production configuration for:

* JWT secret handling
* Helmet
* CORS
* HPP
* rate limiting
* request logging
* centralized error handling
* password handling
* authorization checks
* validation middleware

Make sure errors do not expose stack traces or secrets in production.

Do not weaken any existing security controls just to make deployment work.

### 9. Cloudinary

Verify that Cloudinary credentials are read from environment variables.

Do not hardcode:

CLOUDINARY_CLOUD_NAME
CLOUDINARY_API_KEY
CLOUDINARY_API_SECRET

Verify that profile-image upload stores the Cloudinary URL in PostgreSQL as intended.

Do not store uploaded image binary data in PostgreSQL.

### 10. Account cleanup/background jobs

Inspect the existing account cleanup/background-job implementation.

Make sure it will not accidentally delete production data immediately after deployment.

If a job runs automatically on startup, verify its retention logic and safety conditions.

Do not disable existing functionality unless there is a genuine deployment problem.

### 11. Swagger

Verify that Swagger/OpenAPI works in production without exposing secrets or internal configuration.

Keep the existing `/api-docs` behavior unless there is a security reason to change it.

### 12. Final verification

Run the available backend checks, such as:

npm install
npx prisma generate
npm test

or the project's existing test/lint/type-check commands.

Do not invent test commands if they do not exist.

Then verify that the backend can start successfully in production mode without requiring local-only files.

### 13. Final report

After completing the audit, give me a concise report with:

1. Files changed
2. Production issues fixed
3. Environment variables required by Render
4. Exact Render Build Command
5. Exact Render Start Command
6. Whether Prisma migrations are ready
7. Whether `.env`/secrets are protected
8. Any remaining issue that I must fix manually

Do not deploy anything yet.

Do not create a Render service.

Only prepare and verify the backend for safe deployment.
