node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Runtime log output (was previously tracked; existing logs/*.log files
# remain in git history/tracking until removed with `git rm --cached`)
logs/*.log
